import styled from "@emotion/styled";

export const FiterBtn = styled.button`
  display: block;
  margin-bottom: 10px;
  font-size: 18px;
  background-color: transparent;
  color: ${(p) => p.theme.colors.white};
  border: none;
`;
